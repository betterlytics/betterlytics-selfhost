# ===========================================
# Betterlytics Self-Hosted Configuration
# ===========================================
# Copy this file to .env and fill in the values,
# or run ./setup.sh to generate it interactively.

# --- Proxy / HTTPS ---
ENABLE_HTTPS=false
DOMAIN=analytics.example.com
ACME_EMAIL=admin@example.com
HTTP_PORT=80
HTTPS_PORT=443
# BIND_ADDRESS controls which network interface the ports bind to.
# Behind a reverse proxy (ENABLE_HTTPS=false): use 127.0.0.1 to prevent direct public access.
# Standalone with HTTPS (ENABLE_HTTPS=true): use 0.0.0.0 to accept traffic from all interfaces.
BIND_ADDRESS=127.0.0.1

# --- Database Passwords ---
# Use strong, random passwords (32+ characters recommended)
CLICKHOUSE_PASSWORD=changeme
CLICKHOUSE_BACKEND_PASSWORD=changeme
CLICKHOUSE_DASHBOARD_PASSWORD=changeme
POSTGRES_PASSWORD=changeme

# --- Public URL ---
# The full URL where Betterlytics will be accessible
PUBLIC_BASE_URL=https://analytics.example.com

# --- Auth Secrets ---
# NEXTAUTH_SECRET: random string (64+ characters recommended)
NEXTAUTH_SECRET=changeme

# TOTP_SECRET_ENCRYPTION_KEY: must be exactly 32 characters
TOTP_SECRET_ENCRYPTION_KEY=changeme-must-be-32-characters!!

# --- Admin Account ---
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=admin

# --- General ---
DEFAULT_LANGUAGE=en

# --- Geolocation (optional) ---
ENABLE_GEOLOCATION=false
MAXMIND_ACCOUNT_ID=xxxxx
MAXMIND_LICENSE_KEY=xxxxx
