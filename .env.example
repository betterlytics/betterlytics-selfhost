# ===========================================
# Betterlytics Self-Hosted Configuration
# ===========================================
# Copy this file to .env and fill in the values,
# or run ./setup.sh to generate it interactively.

# --- Domain ---
# The domain where Betterlytics will be accessible (without protocol)
DOMAIN=analytics.example.com

# --- HTTPS ---
# Set to "https" to enable built-in HTTPS via Let's Encrypt.
# Leave "http" if using your own reverse proxy.
HTTP_SCHEME=http

# --- Secret Base ---
# Single secret used to derive all service passwords and auth keys.
# Use a strong, random string (64+ characters recommended).
# All database passwords, NEXTAUTH_SECRET, and TOTP key are derived from this.
SECRET_BASE=changeme

# --- Admin Account ---
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=admin

# --- General ---
DEFAULT_LANGUAGE=en
ENABLE_UPTIME_MONITORING=false

# --- Geolocation ---
ENABLE_GEOLOCATION=false
MAXMIND_ACCOUNT_ID=xxxxx
MAXMIND_LICENSE_KEY=xxxxx
