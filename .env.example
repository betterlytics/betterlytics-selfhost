# ===========================================
# Betterlytics Self-Hosted Configuration
# ===========================================
# Copy this file to .env and fill in the values,
# or run ./setup.sh to generate it interactively.

# --- Proxy / HTTPS ---
SSL_ENABLED=false
HTTP_PORT=80
HTTPS_PORT=443
# BIND_ADDRESS controls which network interface the ports bind to.
# Behind a reverse proxy (SSL_ENABLED=false): use 127.0.0.1 to prevent direct public access.
# Standalone with HTTPS (SSL_ENABLED=true): use 0.0.0.0 to accept traffic from all interfaces.
BIND_ADDRESS=127.0.0.1

# --- Public URL ---
# The full URL where Betterlytics will be accessible
PUBLIC_BASE_URL=https://analytics.example.com

# --- Secret Base ---
# Single secret used to derive all service passwords and auth keys.
# Use a strong, random string (64+ characters recommended).
# All database passwords, NEXTAUTH_SECRET, and TOTP key are derived from this.
SECRET_BASE=changeme

# --- Admin Account ---
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=admin

# --- General ---
DEFAULT_LANGUAGE=en

# --- Geolocation (optional) ---
ENABLE_GEOLOCATION=false
MAXMIND_ACCOUNT_ID=xxxxx
MAXMIND_LICENSE_KEY=xxxxx
